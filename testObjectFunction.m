function [PD, dB, T2] = testObjectFunction (xSpace, ySpace, zSpace, R, voxelSize, B0)
% y = AP
% x = RL
% z = FH
% voxelSize: AP RL FH

[x,y,z] = meshgrid(linspace(-1,1,length(xSpace)),linspace(-1,1,length(ySpace)),linspace(-1,1,length(zSpace)));

ps = [x(:) y(:) z(:)];


PD = zeros(size(x));

% A
PD(-ps(:,1)>0.08 & -ps(:,1)<0.12 & -ps(:,2)>0.6 & -ps(:,2)<0.8) = 1;
PD(-ps(:,1)>-0.12 & -ps(:,1)<-0.08 & -ps(:,2)>0.6 & -ps(:,2)<0.8) = 1;

PD(-ps(:,1)>-0.1 & -ps(:,1)<0.1 & -ps(:,2)>0.78 & -ps(:,2)<0.82) = 1;
PD(-ps(:,1)>-0.1 & -ps(:,1)<0.1 & -ps(:,2)>0.68 & -ps(:,2)<0.72) = 1;

% P
PD(-ps(:,1)>0.08 & -ps(:,1)<0.12 & -ps(:,2)>-0.8 & -ps(:,2)<-0.7) = 1;
PD(-ps(:,1)>-0.12 & -ps(:,1)<-0.08 & -ps(:,2)>-0.8 & -ps(:,2)<-0.6) = 1;

PD(-ps(:,1)>-0.1 & -ps(:,1)<0.1 & -ps(:,2)>-0.82 & -ps(:,2)<-0.78) = 1;
PD(-ps(:,1)>-0.1 & -ps(:,1)<0.1 & -ps(:,2)>-0.72 & -ps(:,2)<-0.68) = 1;

% R
PD(ps(:,2)>0.08 & ps(:,2)<0.12 & ps(:,1)>-0.8 & ps(:,1)<-0.6) = 1;
PD(ps(:,2)>-0.12 & ps(:,2)<-0.08 & ps(:,1)>-0.8 & ps(:,1)<-0.7) = 1;

PD(ps(:,2)>-0.1 & ps(:,2)<0.1 & ps(:,1)>-0.82 & ps(:,1)<-0.78) = 1;
PD(ps(:,2)>-0.1 & ps(:,2)<0.1 & ps(:,1)>-0.72 & ps(:,1)<-0.68) = 1;

PD(ps(:,2)>-0.02 & ps(:,2)<0.02 & ps(:,1)>-0.7 & ps(:,1)<-0.6) = 1;

% L
PD(-ps(:,2)>0.08 & -ps(:,2)<0.12 & -ps(:,1)>-0.8 & -ps(:,1)<-0.6) = 1;
PD(ps(:,2)>-0.1 & ps(:,2)<0.1 & -ps(:,1)>-0.62 & -ps(:,1)<-0.58) = 1;

% F
PD(ps(:,1)>0.08 & ps(:,1)<0.12 & ps(:,3)>-0.8 & ps(:,3)<-0.6) = 1;
PD(ps(:,1)>-0.1 & ps(:,1)<0.1 & ps(:,3)>-0.82 & ps(:,3)<-0.78) = 1;
PD(-ps(:,1)>-0.1 & -ps(:,1)<0.05 & ps(:,3)>-0.72 & ps(:,3)<-0.68) = 1;

% H
PD(ps(:,1)>0.08 & ps(:,1)<0.12 & -ps(:,3)>-0.8 & -ps(:,3)<-0.6) = 1;
PD(-ps(:,1)>0.08 & -ps(:,1)<0.12 & -ps(:,3)>-0.8 & -ps(:,3)<-0.6) = 1;
PD(ps(:,1)>-0.1 & ps(:,1)<0.1 & -ps(:,3)>-0.72 & -ps(:,3)<-0.68) = 1;

dB = 0;
T2 = Inf;

end

